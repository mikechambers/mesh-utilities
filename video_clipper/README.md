Takes a folder of video clips and splits them into smaller clips and saves to the source folder.

## Installation

```
brew install ffmpeg
```

## Usage

```
python3 video_clipper.py ./source/ ./output/ --duration 2
```
